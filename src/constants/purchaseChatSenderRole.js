const PURCHASE_CHAT_SENDER_ROLE = Object.freeze({
  SALES: 'sales',
  PURCHASE: 'purchase',
  ADMIN: 'admin',
});

const PURCHASE_CHAT_SENDER_ROLE_VALUES = Object.values(PURCHASE_CHAT_SENDER_ROLE);

module.exports = {
  PURCHASE_CHAT_SENDER_ROLE,
  PURCHASE_CHAT_SENDER_ROLE_VALUES,
};
