/** Member gender — model + Joi (Phase 2) */
const MEMBER_GENDER = Object.freeze({
  MALE: 'male',
  FEMALE: 'female',
});

const MEMBER_GENDER_VALUES = Object.values(MEMBER_GENDER);

module.exports = { MEMBER_GENDER, MEMBER_GENDER_VALUES };
