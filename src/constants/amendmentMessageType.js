const AMENDMENT_MESSAGE_TYPE = Object.freeze({
  TEXT: 'text',
  DOCUMENT: 'document',
  IMAGE: 'image',
});

const AMENDMENT_MESSAGE_TYPE_VALUES = Object.values(AMENDMENT_MESSAGE_TYPE);

module.exports = {
  AMENDMENT_MESSAGE_TYPE,
  AMENDMENT_MESSAGE_TYPE_VALUES,
};
