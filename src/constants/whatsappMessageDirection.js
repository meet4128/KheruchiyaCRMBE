const WHATSAPP_MESSAGE_DIRECTIONS = ['inbound', 'outbound'];

const WHATSAPP_MESSAGE_DIRECTION = {
  INBOUND: 'inbound',
  OUTBOUND: 'outbound',
};

module.exports = {
  WHATSAPP_MESSAGE_DIRECTIONS,
  WHATSAPP_MESSAGE_DIRECTION,
};
